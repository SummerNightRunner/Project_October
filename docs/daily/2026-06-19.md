# Дневник дня - 2026-06-19

Обновлено Project Secretary: 2026-06-19 11:56 MSK.

## Текущее состояние

- Ветка `main`; рабочее дерево было чистым по `git status --short` до Project Secretary-обновления.
- Фаза 0 завершена: структура данных, базовая документация, FastAPI-каркас и smoke-тесты восстановлены.
- Фаза 1 backend API закрыта для базового ручного ввода: есть `GET /health`,
  `GET /movies/search`, `POST /recommendations`, endpoints истории, оценок и
  ручных предпочтений, API-key auth и owner-bound доступ к user-scoped endpoints.
- DB-контур пользовательской истории реализован: SQLAlchemy/Alembic, миграции таблиц, синхронизация `movie_catalog_entries` с обработанным каталогом.
- `API-006` выполнен: preferences endpoints добавлены без изменения
  рекомендательной модели.
- `POST /recommendations` пока публичный; это временное решение до отдельной модели публичного API-доступа, usage logs, лимитов и тарифов.
- Последняя подтвержденная проверка после `API-006`: `python -m pytest` во
  временном venv прошел, 47 тестов успешны, 1 предупреждение Starlette о
  deprecated `httpx` import.

## Рекомендуемые задачи на сегодня

1. `DEV-001`: добавить Docker Compose для локального backend MVP.
2. `DEV-002`: добавить demo seed для локальной ручной проверки.
3. `WEB-002`: создать React/Vite frontend shell после стабилизации backend запуска.
4. `OCT-003`: описать текущий Python-прототип и точки входа.
5. `TEST-002`: убрать или явно зафиксировать предупреждение Starlette/httpx.

## Первая рекомендуемая задача

`DEV-001`: добавить Docker Compose для локального backend MVP.

Почему первая: после `API-006` backend имеет базовые endpoints ручного ввода, но
для frontend MVP нужен воспроизводимый локальный запуск FastAPI, PostgreSQL,
миграций, синхронизации каталога и demo-доступа.

Граница задачи на сегодня: подготовить воспроизводимый локальный backend-контур,
не начиная frontend и не добавляя production-секреты или реальные пользовательские
данные.

Ожидаемый результат:

- Локально одной командой поднимаются FastAPI и PostgreSQL.
- Есть понятный порядок применения Alembic migrations и синхронизации
  `movie_catalog_entries`.
- Подготовлена база для `DEV-002` demo seed и дальнейшего frontend-подключения.

## Риски и блокеры

- Service clients отложены из ближайшего MVP: clients с `owner_user_id = NULL` должны оставаться запрещенными для user-scoped endpoints.
- Публичный `POST /recommendations` не готов для внешнего production-доступа без API-key модели, rate limits и usage logs.
- `data/processed/processed_metadata.csv`, `data/key/secret.key`, локальные базы, embeddings, vector indexes и ML-артефакты не должны попадать в Git.
- Предупреждение Starlette/httpx не блокирует ближайшую работу, но будет шуметь при росте тестов.
- Frontend stack выбран: React + Vite. Frontend-каркас еще не создан.
- Docker Compose пока не реализован; без `DEV-001` frontend-разработка будет зависеть от ручного запуска backend, PostgreSQL, миграций и seed-данных.

## Принятые рабочие решения

- Первым рабочим фокусом остается `DEV-001`; `DEV-002` идет сразу после него.
- Demo seed нужен не только для одного пользователя, а для нескольких базовых сценариев ручной проверки: история, оценки, предпочтения и получение рекомендаций.
- Docker Compose может использовать стандартные локальные PostgreSQL-значения без production-секретов; такие значения должны оставаться явно локальными и безопасными для коммита.

## Решения, которые нужны от пользователя

- Новых решений перед стартом `DEV-001` не требуется.

## Проверки

- `git status --short`: чисто перед обновлением документации.
- `API-006`: `python -m pytest tests/test_user_history_api.py` прошел, 30
  тестов успешны.
- `API-006`: полный `python -m pytest` прошел, 47 тестов успешны.
- `API-006` подтвержден Project HQ после merge:
  `/tmp/project_october_api006_hq_venv/bin/python -m pytest` прошел, 47 тестов
  успешны, 1 предупреждение Starlette о deprecated `httpx` import.
