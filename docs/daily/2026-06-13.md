# Дневник дня - 2026-06-13

## Текущее состояние

- Репозиторий приведен к чистому базовому состоянию после хаотичного результата задачи.
- `backend/app/main.py` теперь минимальный FastAPI app с `GET /health`.
- `requirements.txt` добавлен.
- `docs/` восстановлены как проектная память.
- `data/raw/` принято как целевая директория исходных CSV через Git LFS.
- `data/processed/`, секреты, файлы промптов и локальные ML-артефакты не коммитятся.
- `DB-001` зафиксировал проектную PostgreSQL-схему пользовательской истории и API-доступа.
- `DB-003` реализовал SQLAlchemy-модели и Alembic-миграцию этой схемы.
- `DB-004` добавил синхронизацию `movie_catalog_entries` с обработанным каталогом.
- `API-004` добавил минимальные endpoints пользовательской истории и оценок.
- `API-005` добавил API-key auth для endpoints пользовательской истории и оценок.

## Сделано

- [x] Восстановить проектную документацию.
- [x] Зафиксировать политику данных.
- [x] Создать минимальный каркас FastAPI.
- [x] Добавить файл зависимостей.
- [x] `TEST-001`: добавить дымовые тесты для `GET /health` и примера рекомендаций.
- [x] Подтвердить `TEST-001` из Project HQ: `python -m pytest` прошел, 2 теста успешны.
- [x] `API-002`: добавить `POST /recommendations` поверх текущей рекомендательной функции.
- [x] Подтвердить `API-002` из Project HQ после merge: `python -m pytest` прошел, 5 тестов успешны.
- [x] `API-003`: добавить `GET /movies/search` по локальному обработанному каталогу.
- [x] Подтвердить `API-003`: `python -m pytest` прошел, 9 тестов успешны.
- [x] `DB-001`: спроектировать PostgreSQL-схему пользовательской истории.
- [x] Подтвердить `DB-001` из Project HQ после merge: `python -m pytest` прошел, 9 тестов успешны.
- [x] `DB-002`: добавить базовую DB-инфраструктуру PostgreSQL без пользовательских таблиц.
- [x] Подтвердить `DB-002` из Project HQ после merge: `python -m pytest` прошел, 11 тестов успешны.
- [x] `DB-003`: реализовать миграции схемы пользовательской истории.
- [x] Подтвердить `DB-003` из Project HQ после merge: `python -m pytest` прошел, 13 тестов успешны; `alembic history` видит новую ревизию.
- [x] `DB-004`: синхронизировать `movie_catalog_entries` с обработанным каталогом через upsert без удаления пропавших строк.
- [x] Подтвердить `DB-004`: `python -m pytest` прошел, 17 тестов успешны; CLI entrypoint проверен на fixture CSV и временной SQLite-базе.
- [x] Подтвердить `DB-004` из Project HQ после merge: `python -m pytest` прошел, 17 тестов успешны.
- [x] `API-004`: добавить `GET /users/{user_id}/history`, `PUT /users/{user_id}/history/{movie_id}` и `PUT /users/{user_id}/ratings/{movie_id}`.
- [x] Подтвердить `API-004`: `python -m pytest` прошел через временный `/tmp` alias на Python 3.12 Codex runtime, 24 теста успешны.
- [x] Подтвердить `API-004` из Project HQ после merge: временный venv, 24 теста успешны, 1 предупреждение Starlette.
- [x] `API-005`: добавить API-key auth для сторонних проектов.
- [x] Подтвердить `API-005`: временный venv на bundled Python 3.12, `python -m pytest` прошел, 31 тест успешен, 1 предупреждение Starlette.

## Дальше

1. `API-007`: связать API key/client с допустимыми пользователями, чтобы ключ не давал доступ к произвольному `user_id`.
2. `API-006`: добавить endpoints ручных предпочтений пользователя.
3. `OCT-003`: подробнее описать текущий Python-прототип и точки входа.

## Риски

- Старые данные в `data/*.csv` заменяются на `data/raw/*.csv`; перед push нужно убедиться, что файлы добавлены через Git LFS, а не как обычные большие blobs.
- `data/processed/processed_metadata.csv` локальный генерируемый артефакт и не должен попасть в Git.
- `data/key/secret.key` не должен попасть в Git.
- После `API-005` endpoints истории и оценок проверяют API-key и scopes, но пока не связывают ключ с допустимыми `user_id`.

## Последняя выполненная задача

`API-005`: добавлен API-key auth для `GET /users/{user_id}/history`, `PUT /users/{user_id}/history/{movie_id}` и `PUT /users/{user_id}/ratings/{movie_id}`. Проверяются `Authorization: Bearer <api_key>`, `key_prefix`, `key_hash`, статус ключа, срок действия, статус API client и scopes; при успехе обновляется `last_used_at`. `POST /recommendations` временно оставлен публичным. Проверка во временном venv на bundled Python 3.12: 31 тест успешен, 1 предупреждение Starlette.
