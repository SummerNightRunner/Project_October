# Daily Plan - 2026-06-13

## Текущее состояние

- Репозиторий приводится к clean baseline после хаотичного task-результата.
- `backend/app.py` теперь минимальный FastAPI app с `GET /health`.
- `requirements.txt` добавлен.
- `docs/` восстановлены как проектная память.
- `data/raw/` принято как целевая директория raw CSV через Git LFS.
- `data/processed/`, секреты, prompts и локальные ML-артефакты не коммитятся.

## Сделано

- [x] Восстановить проектную документацию.
- [x] Зафиксировать data policy.
- [x] Создать минимальный FastAPI skeleton.
- [x] Добавить dependency file.
- [x] `TEST-001`: добавить smoke-тесты для `GET /health` и recommendation example.
- [x] Подтвердить `TEST-001` из Project HQ: `python -m pytest` прошел, 2 теста успешны.

## Дальше

1. `API-002`: добавить `POST /recommendations`.
2. `API-003`: добавить `GET /movies/search`.
3. `DB-001`: спроектировать PostgreSQL-схему пользовательской истории.

## Риски

- Старые данные в `data/*.csv` заменяются на `data/raw/*.csv`; перед пушем нужно убедиться, что файлы staged через Git LFS, а не как обычные большие blobs.
- `data/processed/processed_metadata.csv` локальный generated artifact и не должен попасть в Git.
- `data/key/secret.key` не должен попасть в Git.

## Первая следующая задача

`API-002`: добавить `POST /recommendations` поверх текущей recommendation function.
