# Дневник дня - 2026-06-20

Обновлено Project Secretary: 2026-06-20 15:09 MSK.

## Текущее состояние

- Ветка `main`; последний commit `484eebe docs: record local mvp planning decisions`.
- Рабочее дерево было чистым по `git status --short` до Project Secretary-обновления.
- Фаза 0 завершена: структура данных, базовая документация, FastAPI-каркас и smoke-тесты восстановлены.
- Фаза 1 backend API закрыта для базового ручного ввода: есть `GET /health`, `GET /movies/search`, `POST /recommendations`, endpoints истории, оценок и ручных предпочтений, API-key auth и owner-bound доступ к user-scoped endpoints.
- DB-контур пользовательской истории реализован: SQLAlchemy/Alembic, миграции таблиц, синхронизация `movie_catalog_entries` с обработанным каталогом.
- `API-006` выполнен: preferences endpoints добавлены без изменения рекомендательной модели.
- Следующий продуктовый рубеж - локально воспроизводимый backend MVP для ручной проверки и будущего frontend.
- Последняя подтвержденная проверка после `API-006`: `python -m pytest` во временном venv прошел, 47 тестов успешны, 1 предупреждение Starlette о deprecated `httpx` import.

## Рекомендуемые задачи на сегодня

1. `DEV-001`: добавить Docker Compose для локального backend MVP.
2. `DEV-002`: добавить demo seed для локальной ручной проверки.
3. `OCT-003`: описать текущий Python-прототип и точки входа подробнее.
4. `TEST-002`: убрать или явно зафиксировать предупреждение Starlette/httpx в тестовом контуре.
5. `WEB-002`: создать React/Vite frontend shell после стабилизации локального backend запуска.

## Первая рекомендуемая задача

`DEV-001`: добавить Docker Compose для локального backend MVP.

Почему первая: backend уже содержит основные API и DB-контур, но без одной понятной локальной команды разработка frontend и ручная проверка сценариев будут зависеть от ручного запуска FastAPI, PostgreSQL, Alembic migrations и синхронизации каталога.

Граница задачи на сегодня:

- Подготовить локальный Docker Compose-контур для FastAPI и PostgreSQL.
- Описать порядок запуска миграций и синхронизации `movie_catalog_entries`.
- Использовать только локальные безопасные значения окружения без production-секретов.
- Не начинать frontend и не добавлять реальные пользовательские данные.

Ожидаемый результат:

- Локальный backend MVP поднимается воспроизводимо.
- Следующая задача `DEV-002` сможет добавить demo user, API client/key и demo-данные поверх готового контура.
- Документация запуска понятна без обращения к личным настройкам разработчика.

## Риски и блокеры

- `POST /recommendations` пока публичный; это допустимо для локального MVP, но не для production/public API без API-key модели, usage logs, лимитов и тарифов.
- Service clients отложены: clients с `owner_user_id = NULL` должны оставаться запрещенными для user-scoped endpoints.
- `data/processed/processed_metadata.csv`, `data/key/secret.key`, локальные базы, embeddings, vector indexes и ML-артефакты не должны попадать в Git.
- Docker Compose еще не реализован; без `DEV-001` `DEV-002` и frontend будут опираться на ручные шаги.
- Предупреждение Starlette/httpx не блокирует ближайшую работу, но остается техническим шумом в тестах.

## Решения, которые нужны от пользователя

- Новых решений перед стартом `DEV-001` не требуется.
- Позже перед `DEV-002` желательно подтвердить минимальный набор demo-сценариев, если он должен отличаться от уже принятого набора: история, оценки, предпочтения и получение рекомендаций.

## Проверки

- Прочитаны `docs/ROADMAP.md`, `docs/BACKLOG.md`, `docs/JOURNAL.md` и latest `docs/daily/2026-06-19.md`.
- `git status --short`: чисто перед обновлением документации.
- Код, source-файлы, raw/processed data и локальные конфиги не изменялись.
