# Дневник дня - 2026-06-21

Обновлено Project Secretary: 2026-06-21 11:03 MSK.

## Текущее состояние

- Ветка `main`; последний commit `7706b4d docs: add daily plan for 2026-06-20`.
- Рабочее дерево было чистым по `git status --short` до Project Secretary-обновления.
- Фаза 0 завершена: структура данных, базовая документация, FastAPI-каркас и smoke-тесты восстановлены.
- Фаза 1 backend API закрыта для базового ручного ввода: есть `GET /health`, `GET /movies/search`, `POST /recommendations`, endpoints истории, оценок и ручных предпочтений, API-key auth и owner-bound доступ к user-scoped endpoints.
- DB-контур пользовательской истории реализован: SQLAlchemy/Alembic, миграции таблиц, синхронизация `movie_catalog_entries` с обработанным каталогом.
- Ближайший продуктовый рубеж не изменился: локально воспроизводимый backend MVP для ручной проверки и подготовки frontend-контура.
- Последняя подтвержденная проверка после `API-006`: `python -m pytest` во временном venv прошел, 47 тестов успешны, 1 предупреждение Starlette о deprecated `httpx` import.

## Рекомендуемые задачи на сегодня

1. `DEV-001`: добавить Docker Compose для локального backend MVP.
2. `DEV-002`: добавить demo seed для локальной ручной проверки.
3. `OCT-003`: описать текущий Python-прототип и точки входа подробнее.
4. `TEST-002`: убрать или явно зафиксировать предупреждение Starlette/httpx в тестовом контуре.
5. `WEB-002`: создать React/Vite frontend shell после стабилизации локального backend запуска.

## Первая рекомендуемая задача

`DEV-001`: добавить Docker Compose для локального backend MVP.

Почему первая: без воспроизводимого локального контура backend дальнейшие задачи `DEV-002` и `WEB-002` остаются завязаны на ручной запуск PostgreSQL, миграций, синхронизации каталога и FastAPI.

Граница задачи на сегодня:

- Подготовить `docker-compose`/Compose-контур для FastAPI и PostgreSQL.
- Зафиксировать порядок локального старта, миграций и синхронизации `movie_catalog_entries`.
- Использовать только безопасные локальные значения окружения без production-секретов.
- Не переходить к frontend и не добавлять реальные пользовательские данные.

Ожидаемый результат:

- Backend MVP поднимается одной понятной локальной командой.
- `DEV-002` можно выполнять поверх готового контура без проектирования запуска с нуля.
- Документация локального старта достаточно ясна для следующего исполнителя.

## Риски и блокеры

- `POST /recommendations` пока публичный; для локального MVP это приемлемо, но публичный API без ключей, лимитов и usage logs по-прежнему не готов.
- Service clients остаются отложенными: clients с `owner_user_id = NULL` должны и дальше получать `403` на user-scoped endpoints.
- Docker Compose еще не реализован; это главный операционный блокер для demo seed и frontend shell.
- Предупреждение Starlette/httpx не блокирует ближайшую работу, но создает шум в тестовом контуре и будет мешать ежедневной верификации.
- В Git не должны попадать `data/processed/processed_metadata.csv`, `data/key/secret.key`, локальные базы, embeddings, vector indexes и другие машинно-локальные артефакты.

## Решения, которые нужны от пользователя

- Новых решений для старта `DEV-001` не требуется.
- Перед `DEV-002` полезно подтвердить demo-сценарии, только если нужен состав шире уже ожидаемого MVP-набора: история, оценки, предпочтения и рекомендации.

## Проверки

- Прочитаны `docs/ROADMAP.md`, `docs/BACKLOG.md`, `docs/JOURNAL.md` и latest `docs/daily/2026-06-20.md`.
- Проверен `git status --short`: чисто до обновления документации.
- Код, source-файлы, raw/processed data и локальные конфиги не изменялись.
