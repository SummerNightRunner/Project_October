# Дневник дня - 2026-06-26

Обновлено Project Secretary: 2026-06-26 11:11 MSK.

## Текущее состояние

- Ветка `main`; рабочее дерево было чистым до документационных обновлений.
- Фаза 0 завершена.
- Фаза 1 Backend API MVP практически закрыта: есть `GET /health`,
  `GET /movies/search`, `POST /recommendations`, endpoints истории, оценок,
  ручных предпочтений и API-key auth для user-scoped endpoints.
- Фаза 2 продвинута: PostgreSQL-схема пользовательской истории, Alembic
  migration, DB-инфраструктура и синхронизация `movie_catalog_entries`
  реализованы.
- Фаза 3 начата инфраструктурно: `DEV-001` Docker Compose для локального
  backend MVP выполнен и подтвержден.
- Главный следующий разрыв локального MVP: нет demo seed для пользователя,
  API client/key и минимальных demo-данных.

## Рекомендуемые задачи на сегодня

1. `DEV-002` - добавить demo seed для локального MVP.
2. `API-008` - добавить user-based recommendations endpoint после появления
   demo user/data или хотя бы согласовать контракт.
3. `OCT-003` - описать текущий Python-прототип и точки входа подробнее.
4. `TEST-002` - убрать или явно зафиксировать предупреждение Starlette/httpx в
   тестовом контуре.
5. `WEB-002` - создать React/Vite frontend shell после стабилизации demo seed.

## Первая рекомендуемая задача

`DEV-002`: добавить demo seed для локального MVP.

Ожидаемый результат: локальный разработчик может поднять Compose, применить
migrations, синхронизировать каталог, создать demo user/API client/API key и
проверить историю, оценки, предпочтения и будущие user-based рекомендации без
ручного SQL и production-секретов.

## Риски и блокеры

- Для demo seed нужен подтвержденный формат выдачи/хранения demo API key:
  ключ нельзя коммитить как секрет, но локальный сценарий должен быть
  воспроизводимым.
- `data/processed/processed_metadata.csv` остается generated artifact; seed не
  должен предполагать, что он закоммичен.
- `POST /recommendations` пока публичный и не связан с пользовательской
  историей; `API-008` нужен для полноценного сценария рекомендаций по `user_id`.
- Предупреждение Starlette/httpx не блокирует работу, но оставляет шум в
  полной проверке.
- Frontend shell будет полезнее после demo seed, иначе локальный UX придется
  проверять через ручную подготовку базы.

## Решения, нужные от пользователя

- Подтвердить, должен ли `DEV-002` печатать одноразовый demo API key в stdout
  при seed-запуске или использовать предсказуемый локальный ключ только для
  dev-окружения.
- Подтвердить минимальный набор demo-данных: только user/client/key или также
  несколько history/rating/preference записей.
- Решить, начинать ли `API-008` сразу после `DEV-002` или сначала закрыть
  `OCT-003`/`TEST-002`.

## Проверка

- Прочитаны `docs/ROADMAP.md`, `docs/BACKLOG.md`, `docs/JOURNAL.md`, последняя
  daily note `docs/daily/2026-06-25.md` и `git status --short`.
- Код и source-файлы не изменялись.

## Следующая сессия

- Начать с `DEV-002`.
- Перед реализацией проверить существующие DB helpers, API-key hashing,
  README/Compose команды и тестовые fixture patterns.
