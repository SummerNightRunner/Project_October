# Архитектурные решения

## 2026-06-19 - Frontend MVP на React и Vite

Статус: принято.

Решение:

Для MVP сайта использовать React + Vite.

Причина:

Project October сейчас нужен быстрый SPA-интерфейс поверх FastAPI: поиск фильмов, управление историей, оценки, ручные предпочтения и экран рекомендаций. React + Vite дает простой локальный цикл разработки и меньше инфраструктурной сложности, чем Next.js, пока нет требований к SEO, server-side rendering или публичным маркетинговым страницам.

Последствия:

- `WEB-001` считается закрытым решением.
- MVP сайта строится как React/Vite frontend, который обращается к FastAPI backend.
- Next.js остается возможным будущим вариантом, если появятся SEO-страницы, SSR-требования или отдельная публичная web-платформа.

## 2026-06-19 - Service clients не входят в ближайший MVP

Статус: принято.

Решение:

В ближайшем MVP user-scoped endpoints остаются доступны только owner-bound API clients: `api_clients.owner_user_id` должен совпадать с path `user_id`. Clients без владельца (`owner_user_id = NULL`) продолжают получать `403` на endpoints пользовательской истории, оценок и будущих ручных предпочтений.

Причина:

Service clients означают системный или интеграционный доступ не от имени одного конкретного пользователя: админские панели, workers, массовые импорты, партнерские интеграции или доступ к нескольким пользователям. Такой доступ требует отдельной модели ролей, scopes, аудита и ограничений. Для MVP безопаснее не расширять права до появления конкретного сценария.

Последствия:

- В ближайшем MVP пользовательские данные доступны только владельцу соответствующего API client/key.
- Модераторы, админы, организации, support-доступ и service clients остаются отдельными будущими решениями.
- Если позже понадобится админский или интеграционный доступ, нужно спроектировать роли/permissions, audit logs и правила доступа к нескольким пользователям до реализации.

## 2026-06-13 - Чистое базовое состояние вместо продолжения хаотичных результатов задач

Статус: принято.

Решение:

Сначала привести репозиторий к понятному базовому состоянию: данные, документация, `.gitignore`, FastAPI endpoint `GET /health` и текущий рекомендательный прототип.

Причина:

Предыдущие автоматические запуски задач оставили противоречивое состояние: часть документов отсутствовала, API-001 не подтверждалась в текущей рабочей копии, а данные были перенесены без зафиксированной политики.

## 2026-06-13 - Исходные CSV живут в `data/raw/`

Статус: принято.

Решение:

Считать `data/raw/` целевой директорией для исходных CSV. Файлы отслеживаются через Git LFS.

Последствия:

- Backend-код читает исходные данные из `data/raw/`.
- Старые отслеживаемые пути `data/*.csv` заменяются на `data/raw/*.csv`.
- Генерируемые файлы в `data/processed/` не коммитятся.

## 2026-06-13 - PostgreSQL как целевое хранилище

Статус: принято.

Решение:

Для пользовательской истории, рейтингов, API keys и состояния приложения целевым хранилищем будет PostgreSQL.

Последствия:

- CSV остается источником исходных данных о фильмах, но не хранилищем состояния приложения.
- SQLite можно использовать только как временный локальный эксперимент, если это явно согласовано.

## 2026-06-13 - Базовая схема пользовательской истории

Статус: принято.

Решение:

Для DB-001 принята базовая PostgreSQL-схема:

- `users` хранит аккаунты пользователей без проектирования паролей и внешней авторизации на этом этапе;
- `movie_catalog_entries` хранит минимальную проекцию локального каталога для ссылочной целостности;
- `user_movie_history` хранит актуальный факт истории пользователя по фильму;
- `user_movie_ratings` хранит нормализованную оценку пользователя по шкале `0..10`;
- `user_preferences` хранит ручные предпочтения с положительным или отрицательным весом;
- `api_clients` и `api_keys` разделяют сторонний проект и его ключи доступа;
- `user_events` хранит аудит действий и не является источником истины для истории или оценок.

Причина:

Рекомендательному MVP нужно отделить состояние приложения от локальных CSV-артефактов и подготовить хранение истории, оценок, ручных предпочтений и API-доступа без немедленного внедрения миграций.

Последствия:

- Пользовательские данные ссылаются на фильмы через `catalog_movie_id`, который равен строковому `id` из `data/processed/processed_metadata.csv`.
- API-ключи должны храниться только как `key_prefix` и `key_hash`; полный секрет в базе не хранится.
- Следующие backend-задачи должны добавить миграции, подключение PostgreSQL, синхронизацию `movie_catalog_entries` и API endpoints для истории.

## 2026-06-13 - DB-инфраструктура на SQLAlchemy, Alembic и psycopg

Статус: принято.

Решение:

Для backend DB-слоя использовать SQLAlchemy 2.x, Alembic и PostgreSQL-драйвер `psycopg` 3.x. Подключение к базе задается только через переменную окружения `PROJECT_OCTOBER_DATABASE_URL`.

Причина:

Этот стек является стандартным для FastAPI/Python backend, поддерживает явные миграции, PostgreSQL-first подход и не требует хранить секреты или локальные DSN в репозитории.

Последствия:

- FastAPI app не подключается к базе при импорте и продолжает работать без `PROJECT_OCTOBER_DATABASE_URL`.
- SQLAlchemy engine/session создаются лениво только когда DB-код явно запрошен.
- Alembic окружение готово для будущих миграций, но DB-002 не создает пользовательские таблицы из DB-001.
- Реальные модели, миграции пользовательской истории и синхронизация каталога остаются задачами DB-003 и DB-004.

## 2026-06-13 - Миграция пользовательской истории PostgreSQL-first

Статус: принято.

Решение:

DB-003 реализует схему DB-001 через SQLAlchemy ORM-модели и Alembic revision `db003_user_history_schema`. Миграция использует PostgreSQL-типы `uuid`, `citext`, `jsonb` и `text[]`, а также extensions `pgcrypto` и `citext`.

Причина:

Пользовательская история, оценки, предпочтения, API-доступ и события должны иметь явный миграционный контракт до реализации endpoints и синхронизации каталога.

Последствия:

- `Base.metadata` содержит таблицы DB-001 и используется Alembic для будущих проверок схемы.
- `movie_catalog_entries` создана как FK-цель, но ее заполнение из `processed_metadata.csv` остается DB-004.
- API endpoints пользовательской истории не реализуются в DB-003 и остаются API-004.
- SQLite-прогон миграции не является целевым из-за PostgreSQL-специфичных типов; безопасная проверка выполняется через импорт ревизии, `alembic history` и offline SQL generation.

## 2026-06-13 - Синхронизация минимального каталога без удаления строк

Статус: принято.

Решение:

DB-004 синхронизирует `movie_catalog_entries` из `processed_metadata.csv` отдельным сервисом и CLI entrypoint `python -m backend.app.db.sync_movie_catalog`. Синхронизация делает upsert по `catalog_movie_id`, заполняет только минимальные поля FK-проекции и не удаляет строки, которых больше нет в CSV.

Причина:

`movie_catalog_entries` нужна как стабильная FK-цель для пользовательской истории, но не должна становиться копией рекомендательного датасета или хранить модельные признаки. Удаление строк при смене локального CSV может нарушить ссылочную целостность будущей истории и оценок.

Последствия:

- Новые фильмы из CSV добавляются в `movie_catalog_entries`.
- Существующие фильмы обновляют `title_snapshot`, `release_date`, `source_catalog_version` и `updated_at`; `created_at` сохраняется.
- `source_catalog_version` остается `NULL`, если оператор явно не передал стабильную версию через CLI или сервисный параметр.
- Схема DB-003 не меняется, новая миграция не нужна.
- API endpoints пользовательской истории не реализуются в DB-004 и остаются задачей API-004.

## 2026-06-13 - Автосоздание пользователя в write-endpoints истории

Статус: принято.

Решение:

В API-004 `PUT /users/{user_id}/history/{movie_id}` и
`PUT /users/{user_id}/ratings/{movie_id}` автоматически создают активного
пользователя по переданному UUID, если такого пользователя еще нет. `GET
/users/{user_id}/history` не создает пользователя и возвращает пустой список,
если у указанного UUID нет записей истории.

Причина:

В проекте пока нет endpoint для регистрации пользователя и нет auth-слоя.
Автосоздание на write-запросах позволяет проверить ручной ввод истории и оценок
без временных CSV или локальных пользовательских файлов, но избегает побочного
создания состояния при read-запросах.

Последствия:

- API-004 не требует новой миграции: используются таблицы DB-003.
- История и оценки всегда пишутся только для фильмов, существующих в
  `movie_catalog_entries`.
- Полноценная идентификация пользователя, API-key auth, scopes и запрет записи
  в чужую историю остаются задачей API-005.

## 2026-06-13 - Минимальная API-key авторизация для сторонних проектов

Статус: принято.

Решение:

API-005 защищает endpoints пользовательской истории и оценок через
`Authorization: Bearer <api_key>`. Формат ключа для MVP:
`oct_<prefix>_<secret>`. В базе хранится открытый `key_prefix` вида
`oct_<prefix>` для поиска и `key_hash` полного ключа в формате `sha256:<hex>`;
полный ключ не хранится и не возвращается наружу. Проверка полного ключа
выполняется через `hmac.compare_digest`.

Реализованные scopes:

- `history:read`;
- `history:write`;
- `ratings:write`;
- `recommendations:read`.

Защищенные endpoints:

- `GET /users/{user_id}/history` требует `history:read`;
- `PUT /users/{user_id}/history/{movie_id}` требует `history:write`;
- `PUT /users/{user_id}/ratings/{movie_id}` требует `ratings:write`.

Отозванные, истекшие ключи и ключи неактивных API clients возвращают `401`,
потому что для клиента это недействительные credentials. Валидный ключ без
нужного scope возвращает `403`.

Причина:

Задача API-005 должна убрать полностью публичную запись пользовательской истории
и подготовить основу для сторонних проектов, не вводя dashboard, billing,
rate limits или полноценную пользовательскую авторизацию.

Последствия:

- `api_keys.last_used_at` обновляется только после успешной проверки ключа и
  требуемого scope.
- PostgreSQL-схема DB-003 не меняется; новая миграция не нужна.
- `POST /recommendations` временно остается публичным, несмотря на наличие
  `recommendations:read`, до появления публичного API-тарифа, лимитов и
  устойчивого решения о доступе к рекомендательному endpoint.
- API-005 не решает привязку `user_id` к владельцу ключа: сторонний проект с
  нужным scope все еще обращается к явно переданному UUID. Это нужно закрыть
  отдельной задачей после появления модели пользователей/клиентов.

## 2026-06-13 - User-scoped API key доступ через `api_clients.owner_user_id`

Статус: принято.

Решение:

В API-007 user-scoped endpoints используют `api_clients.owner_user_id` как
MVP-границу доступа. Ключ валидного активного API client может читать или
писать только тот `user_id`, который совпадает с `owner_user_id`.

Если `owner_user_id` равен `NULL`, доступ к user-scoped endpoints запрещен с
`403`. Service clients, роли, организации и доступ одного клиента к нескольким
пользователям не реализуются в MVP.

`api_keys.last_used_at` обновляется после успешной проверки ключа и требуемого
scope, даже если последующая проверка `owner_user_id` возвращает `403`.

Причина:

После API-005 валидный ключ с нужным scope все еще мог обратиться к
произвольному UUID пользователя. Использование существующего поля
`owner_user_id` закрывает этот риск без новой миграции и без преждевременного
проектирования ролей или организаций.

Последствия:

- `GET /users/{user_id}/history` требует `history:read` и совпадения
  `owner_user_id` с path `user_id`.
- `PUT /users/{user_id}/history/{movie_id}` требует `history:write` и
  совпадения владельца.
- `PUT /users/{user_id}/ratings/{movie_id}` требует `ratings:write` и
  совпадения владельца.
- Валидный ключ с нужным scope, но без доступа к конкретному `user_id`,
  возвращает `403` без раскрытия существования пользователя.
- PostgreSQL-схема DB-003 не меняется; новая миграция не нужна.
